# SoundInsight 项目封包声明（D13）

> **用途**：提交技术质检方（Kimi）最终审核。
> **声明方**：DSH（执行方）｜**项目**：SoundInsight 蓝牙耳机音质差评智能归因系统｜**团队**：更新世界的锋芒（队长：黎同竣）
> **日期**：D13（按 D1 = 9/1 推算约为 2026-09-11；日期为推算值，非文档记录值）
> **封包动作**：将复赛全部工程、验证与材料状态冻结为可提交形态，并完成官方模板格式对照。

---

## 一、声明目的与范围

本声明说明三件事：
1. **D13 当天做了什么**（逐项，含证据文件）；
2. **封包后提交物的可校验状态**（文件名、大小、SHA256 前 16 位、条目数）；
3. **哪些内容被冻结、哪些偏差仍然存在**（如实清单，不隐藏不利信息）。

所有实验数字的权威源为 `results_summary.md`（冻结口径）；本声明中的"今日实测"数字均为本次运行输出，未做任何修饰。

---

## 二、D13 完成事项（逐项 + 证据）

| # | 事项 | 结果 | 证据文件 |
|---|---|---|---|
| 1 | 团队信息表补全 | 成员表按模板字段填齐：黎同竣 / 19195907942 / 2799920054@qq.com / **个人团队** / **个人开发者** / 队长 | `competition_v4.md` §一、`competition_v3.txt` §一 |
| 2 | 演示视频脚本扩至 3 分钟 | 由 130 秒（2 分 10 秒）扩写为 **200 秒（3 分 20 秒）**，新增"验证体系""局限性自述"两个镜头，落入模板建议的 3-5 分钟区间 | `video_script.md`（9 个镜头 + 弹性伸缩说明） |
| 3 | 其他材料包打包 | 生成 `更新世界的锋芒_SoundInsight_其他材料.zip`：46 个文件 + 说明文件（验证报告 10 份、审计与验收脚本 4 个、人工复核原始表 3 张、图表 7 张、复算脚本 13 个、过程文档 5 份） | 打包脚本 `build_submission.py`（`make_other_zip()`） |
| 4 | **主文档生成方式更换**（原 Typora 导出路线失效） | 改为脚本生成，两条路线均可提交：① `python md_to_pdf.py` → PDF（**内嵌宋体/黑体**，不依赖阅读器字体，9 页 201,416 B）；② `python md_to_docx.py` → Word（48,138 B，WPS 可直接打开并一键输出 PDF）。模板明确允许 `.docx 或 .pdf` | `md_to_pdf.py`、`md_to_docx.py`、`verify_docx.py` |
| 5 | 提交包同步 | `复赛作品.zip` 现含 4 个条目：主文档 PDF + Demo.zip + 其他材料.zip + README_SUBMISSION.txt，**仅缺演示视频**（次日已补入，见 §十 补记 4；包内说明亦于当日按实际内容纠错重打，见补记 5） | `pack_final.py`（自动打包 + 自检） |
| 6 | 模板格式自检写入审计 | `check_doc_numbers.py` 新增"模板格式对照"节：官方模板九章齐备、章节编号唯一、在线链接填写表、团队信息已填 | `number_audit.md` 末节 |
| 7 | 提交物清单与命名规范同步 | v3/v4 的提交物清单勾选状态、在线链接填写表、附件命名规范（新增第 4 项"其他材料"）全部更新 | `competition_v4.md` §十一/§十二、`competition_v3.txt` §七/§八 |
| 8 | 全量数字审计 | **0 FAIL / 143 PASS**（含新增模板自检 8 项） | `number_audit.md` |
| 9 | 项目文件整理（同日完成） | 新增 `docs/file_inventory.md`（全部文件分类索引）；删除冗余 4 项（18.5MB PPT 旧备份、临时测试输出、旧版报告、`__pycache__`）；`.gitignore` 补 `*.bak` | `docs/file_inventory.md` §九 |
| 10 | **代码仓库补齐为双平台** | 按模板"GitCode 代码仓库（可选）"栏位要求，新增 GitCode 镜像仓库 https://gitcode.com/DaiYanQBZ95Doll/soundinsight （公开可访问，已用 `git ls-remote` 验证）；v3/v4 的提交物清单与在线链接表均改为 **GitCode + GitHub 双链接** | `competition_v4.md` §十一、`competition_v3.txt` §七 |

---

## 三、提交物校验值（封包后实测）

下表分两类：**内容类**（PDF / Word / 视频 / Demo / 源文档）哈希与重新打包无关，可直接复核；**容器类**（外层 zip、`其他材料.zip`——后者内含本文档）哈希每次重打都会变，且无法把"自己的最终哈希"写进自己内部，故不在此固定，改由包内 `hashes.txt` 与提交页记录。

| 文件 | 大小 | SHA256（前 16 位） | 条目数 |
|---|---|---|---|
| `更新世界的锋芒_SoundInsight_复赛作品.zip`（**已含全部四项 + 包内 `hashes.txt`，可直接提交**） | 容器类 | 见根目录 `hashes.txt` / 提交页 | 6 个条目 |
| `更新世界的锋芒_SoundInsight_复赛作品.pdf`（主文档，内嵌中文字体，含双仓库链接） | 215,103 B | `7a92e5cc8485870a` | 9 页 |
| `更新世界的锋芒_SoundInsight_复赛作品.docx`（Word 备用主文档，WPS 可直接打开） | 49,186 B | `1d1163edb1f03f9a` | 165 段 / 8 表 |
| `更新世界的锋芒_SoundInsight_演示视频.mp4`（1920×1080 / 30fps / 3 分 23 秒） | 48,375,526 B | `9664f7f9c597d061` | 6,091 帧 |
| `更新世界的锋芒_SoundInsight_Demo.zip` | 109,662 B | `2c39e1fabc104d1a` | 51 个文件 |
| `更新世界的锋芒_SoundInsight_其他材料.zip`（内含本文档，故属容器类） | 容器类 | 见包内 `hashes.txt` | 46 个文件 + 1 说明 |
| `competition_v4.md`（主文档源） | 20,571 B | `535eb3df0f1857d1` | 13 章 |
| `video_script.md`（视频脚本，字幕已按实拍重排） | 5,886 B | `02dac8f08e5cc4e0` | 9 镜头 |
| `SoundInsight：跨境电商耳机音质差评智能归因系统.pptx`（口径标注已修正，见补记 11） | 18,935,790 B | `7201d5c255d16334` | 20 页 |

> 说明：`pack_final.py` 每次打包都会 ① 用 `build_submission.py` 的正文重写包内 README（避免"说明与包内容矛盾"再次发生）；② 在根目录与包内各写一份 `hashes.txt`，登记四项提交物的完整 SHA256。
>
> **复核方式**：解包 `复赛作品.zip` → 对四个文件算 SHA256 → 与包内 `hashes.txt` 对照（该清单只登记内容类文件，不登记外层 zip 自身）；外层 zip 的哈希见根目录 `hashes.txt` 与天池提交页。各轮重打的实测值按日期记于 §十 补记 5（历史快照，不再变动）。若之后修改任何主文档，需重跑 `md_to_pdf.py` + `build_submission.py` + `pack_final.py`。

---

## 四、可复现验证（命令 + 实测结果）

```powershell
cd C:\deepseek-harness-master\soundinsight
python check_doc_numbers.py     # 数字审计 + 模板格式对照
python build_submission.py      # 重建三个提交包
python deploy_check.py https://daiyanqbz95doll-soundinsight.ms.show
python batch_check.py https://daiyanqbz95doll-soundinsight.ms.show
```

| 验证项 | 实测结果（封包时刻） |
|---|---|
| 数字一致性审计 | **0 FAIL / 143 PASS**（12+ 文件；含必含数字、违禁 claim、口径一致性） |
| 模板格式对照 | v4：13 章、九章齐备、编号无重复、在线链接表 PASS、团队信息已填 PASS；v3：9 章、同四项 PASS |
| 提交包构建 | Demo.zip 50 文件；其他材料.zip 46+1；复赛作品.zip 3 条目 |
| 在线 Demo 验收（历史记录，同一部署） | 页面 200；`single_predict` 2/2 正确（负面→98.9%+杂音、正面→正常，含校准提示）；`batch_analyze` 100 条 15.1 秒，12 条差评、五类分布与本地一致 |
| 文件整理 | 无重复章节编号、`feedback_template.md` 已删除且引用清零 |

---

## 五、冻结与红线声明（封包后不再改动）

以下内容在封包后**未改动、也不会改动**：

1. 模型权重目录 `sound_model/`、`multi_label_model/`（未重训、未替换）；
2. 固定验证集 `val_v2.csv`（20,000 条 / 251 正例）；
3. 全部训练脚本（`train_final.py`、`train_multilabel.py`、`distilbert_cv.py`、`ablation_train.py`、`learning_curve.py` 等）；
4. `results_summary.md` 中的冻结数字（F1 0.6871、阈值 0.9744、CV 0.6234±0.0240、AUC-PR 0.7191、p=0.000932、基线 0.497/0.410/0.025、多标签宏 F1 0.6481）；
5. 正例口径演进只做记录不做改写：1,257（冻结实验口径）→ 1,288（高音补捞）→ 1,280（中置信剔除）；
6. 模型权重不入 git（`.gitignore` 已排除）；
7. GPU 任务全程串行执行。

---

## 六、已知偏差与未完成事项（如实清单）

| # | 事项 | 状态 |
|---|---|---|
| 1 | PPT 20 页 | 高于赛事建议的 8-12 页；**用户已决定不精简**，`audit_ppt.py` 仍将其标为 FAIL（事实记录，非隐瞒） |
| 2 | 真实用户验证 | **未开展**（无真实用户访谈/试用反馈）；已写入 `competition_v4.md` §9.1 第 7 条 |
| 3 | 主文档 | **已生成并进包**（PDF 9 页，内嵌中文字体；另有 Word 版备用）。原计划的 Typora 导出路线因产物无法打开已废弃，改用脚本生成 |
| 4 | 演示视频文件 | 封包当时**待录制**；**D14 已完成并入包**（成片 3 分 23 秒 / 1920×1080 / 30fps / 46.1 MB，无口播"屏幕录制 + 字幕"方案，字幕 26 条按实拍切点重排，见 §十 补记 4） |
| 5 | 高音归因 F1 = 0 | 样本仅 84 条且与清晰度语义重叠（**零触发**：最高概率 0.3332 < 0.5，TP0/FP0/FN13）。已定位原因（见 `docs/treble_failure_analysis.md`）并选定修复方案 C，**尚未启动**，目标决赛路演（23 日）前 |
| 6 | 长评论截断 | >128 token 桶 F1 0.565（召回 54.4%），已量化未修复 |
| 7 | 标注噪声率 9.1% | 人工终审 15/16 确认；为候选集确认率下的下界估算 |
| 8 | 多语言 | 仅显式拒绝，未支持 |
| 9 | 漂移监控 | 方案已定义（`docs/drift_plan.md`），未在真实流量运行 |
| 10 | D4 无独立记录 | 仓库无 D4 留档，已如实注明（未补造） |
| 11 | GitHub 推送 | 封包前本地有未推送 commit；**D14 已同步**（GitCode 已到 `ae0b88b`，GitHub 因网络中断曾重试，若仍落后执行 `git push origin main`） |

---

## 七、诚信声明

1. 本项目**未虚构任何数据**：所有指标均由脚本产出并留存原始输出（`docs/year_split_output.txt`、`exp01-09_*`、`llm_*_out_*.jsonl` 等）。
2. **未虚构任何用户证言**：`feedback_template.md` 已删除，文档中明确声明"未开展用户验证"。
3. **不利结论全部保留**：校准过度自信（ECE 0.0122 但决策区间偏差达 −0.42）、qwen 在 5-shot/复核模式反超 deepseek、标注噪声 9.1%、长度截断性能下降、D4 无记录等，均如实写入文档。
4. **口径分离**：冻结口径（val_v2 20,000 条）、1,000 条子集口径（LLM 对照）、工程数字（吞吐/校准/长度）在文档中分区标注，明确"不可混用"。
5. **密钥纪律**：Token Plan key 仅用于宿主侧调用，未写入任何文件、未进入 git；已建议用户轮换。
6. **无诱导行为**：全程只报告事实与状态，未替用户做产品/时间决策。

---

## 八、给 Kimi 的最终审核清单（逐条可执行）

- [ ] 跑 `python check_doc_numbers.py`，确认 **0 FAIL**，并检查 `number_audit.md` 末节"模板格式对照"四项 PASS。
- [ ] 打开 `competition_v4.md`，核对模板九章是否齐备、章节编号是否唯一（十一/十二/十三）、§一 成员表字段是否与模板一致。
- [ ] 核对 §5.2 两个模型的"调用方式/链接"是否与 `llm_baseline.md` 的实验记录一致（qwen3.7-plus = 百炼 Token Plan；deepseek-chat = 官方 API）。
- [ ] 抽查冻结数字：F1 0.6871 / 阈值 0.9744 / CV 0.6234±0.0240 / p=0.000932 / 多标签宏 F1 0.6481 是否与 `results_summary.md` 完全一致。
- [ ] 检查"已知偏差"是否与 `audit_ppt.py`（页数 FAIL）和 `competition_v4.md` §9.1（第 7 条未开展用户验证）一致。
- [ ] 复核 `video_script.md` 时长合计是否 ≥180 秒，且口播数字是否全部取自冻结口径。
- [ ] 打开 `更新世界的锋芒_SoundInsight_其他材料.zip`，确认含 `README_其他材料.txt` 与三类证据（验证报告 / 人工复核原始表 / 复算脚本）。
- [ ] 复核 §三 校验值是否与封包文件一致（若已放入 PDF 与视频，`复赛作品.zip` 的 SHA256 应有变化，属预期）。
- [ ] 确认 `SoundInsight_models` 与创空间 URL 均可公开访问（无需登录）。
- [ ] 确认仓库内不存在任何形态的用户证言或"已收集反馈"表述（全文检索"用户反馈/试用/证言"应为 0 命中或仅"未开展"类声明）。

---

## 九、口径与声明边界

1. 本声明由执行方（DSH）出具，**不替代技术质检方的独立判断**；文中"通过/PASS"指脚本化检查通过，不代表第三方复核结论。
2. 日期为推算值：仓库最早记录为 D1 = 9/1，故 D13 推为 9/11；若与实际日历不符，以团队日程为准。
3. 校验值为封包时刻快照；PDF/视频补入后需重算。
4. 本声明不构成任何对外承诺或商业承诺。

---

## 十、封包后补记（D14，2026-09-14）

封包（D13）之后发生了以下变更，均已在对应文档同步，本节作为封包声明的更新附注：

| 变更 | 内容 | 同步文档 |
|---|---|---|
| 1. 产品改进 | 批量分析页由"摘要"升级为**完整六节报告**（与命令行 Agent 同口径，含五类归因概率、中置信提示）；抽出共享模块 `report_builder.py`，语种判定统一到 `text_utils.py` | `competition_v4.md` §4.2/§4.3/§9.2、`PROGRESS_SYNC.md`、`AI_HANDOFF/05` |
| 2. 线上重新部署 | 用户触发重建 → 新镜像 `363008-453f56d8-2026-09-14-17-55-37`；线上实测：批量报告 1,622 字符（六节齐全 + 归因概率 + 三条紧急建议 + 中置信 4 条）、单条推理 2/2、100 条 20.1 秒 | `DEPLOY_GUIDE.md` 更新记录、`AI_HANDOFF/06` 待办 2b |
| 3. 双平台同步 | 本地 / GitHub / GitCode 三处一致：`4735121`（后续文档提交另计） | `AI_HANDOFF/06` 待办 3 |
| 4. **演示视频完成并入包** | 成片 1920×1080 / 30fps / **3 分 23 秒**（6,091 帧 / 46.1 MB）；实际镜头切点 0:20 / 0:33 / 1:11 / 1:42.080 / 2:18.140 / 2:38.140 / 3:03.010 / 3:23.010；字幕经脚本按实拍时间轴重排为 **26 条**（末条 3:23.010），已抽帧核验画面与字幕同步 | `video_script.srt`、`视频录制速查.md`、`AI_HANDOFF/06` 待办 1 |
| 5. **包内说明纠错 + 重打封包** | 包内 `README_SUBMISSION.txt` 原写有"请手动放入最终 PDF / 演示视频"，与包内实际已含这两项不符 → 按实际内容重写（五项内容清单 + 在线 Demo / 双仓库链接）；同时清掉 Demo.zip 中误入的 12 个开发脚本（63 → 51 文件）。`pack_final.py` 现在每次打包都从 `build_submission.py` 重写该 README，并写出 `hashes.txt`（根目录 + 包内），避免说明与校验值再度漂移 | `build_submission.py`、`pack_final.py`、本节 §三 |
| 6. 容器类校验值改为不固定登记 | 外层 zip 与 `其他材料.zip`（内含本文档）属"自引用容器"，其哈希每次重打必变，若写死在文档里必然滞后一次 → §三 改为按类登记：内容类固定哈希，容器类指向 `hashes.txt` 与提交页 | 本节 §三 |
| 7. 全量数字审计复跑 | `python check_doc_numbers.py` → **0 FAIL / 143 PASS**（含模板格式对照 8 项全 PASS） | `number_audit.md` |
| 8. **主文档 §7.2 阈值口径混用修正（用户指出）** | 原文"F1=0.6871（阈值 0.97），召回率 89.6%，精确率 47.9%"把两个阈值的指标并排：89.6%/47.9% 来自阈值 0.5，0.6871 来自阈值 0.9744 → §7.2 改为**按阈值分列的双档表**（0.9744：P 66.3%/R 71.3%/F1 0.6871；0.5：P 47.9%/R 89.6%/F1 0.6241）+ 成对阅读说明；§3.3 价值表、§7.6 消融表的召回/精确也标注阈值档位 | `competition_v4.md` §3.3/§7.2/§7.6、`competition_v3.txt`、`README.md`、`QWEN_HANDOFF.md`、`PROJECT_BRIEF_QWEN.md` |
| 9. **防复发检查入审计** | `check_doc_numbers.py` 新增"口径配对检查（阈值并排）"：一行同时出现调优档 F1（0.687/0.6871）与阈值 0.5 档 P/R（89.6/47.9）时，必须显式写出两个阈值标签，否则 FAIL；覆盖 v4/v3/README/QWEN_HANDOFF/MODEL_CARD/AI_HANDOFF 01·03，PPT 文本单列"注意"（不在提交包内） | `check_doc_numbers.py`、`number_audit.md`（0 FAIL / **150 PASS**） |
| 10. 打包脚本抗占用 | `pack_final.py` 的替换动作改为带重试（Windows 杀软/压缩软件占用 zip 时等待重试，不再直接失败） | `pack_final.py` |
| 11. **PPT 同类口径混用一并修正** | 全量数字审计新增的配对检查在 PPT 文本里也命中同型问题（不在提交包内，本可只提示）：slide10 首段"F1=0.6871、召回率89.6%"与主指标卡"精确率47.9%与召回率89.6%的帕累托平衡点"、slide12 消融表"帕累托最优，召回 89.6%"、slide15 价值表"89.6%（量化）" → 分别改为同档数字（66.3%/71.3%）与阈值标注（"阈值 0.5 档"/"调优 F1 为阈值 0.97 档"）。做法：`fix_ppt_threshold.py` 以整段精确映射改 XML 文本，其余字节不动，先备份 `.pptx.bak`；改动仅 4 张幻灯片的相关段落，页数仍 20 页 | `fix_ppt_threshold.py`、`ppt_text_dump.md`（配对检查 注意项归零） |

**各轮重打实测值（日期化的历史快照，不再变动）**：主文档 PDF 先在功能描述更新时重生成（**208,471 B / `77dfc6d48ace3611`**），后又在 §7.2 口径修正后重生成（**215,103 B / `7a92e5cc8485870a`**，9 页）；`Demo.zip` 依次为 107,294 B（50 文件）→ 109,550 B `da1f0f514d50ecf1`（51 文件）→ **109,662 B `2c39e1fabc104d1a`**（README 口径修正后）；`其他材料.zip` 依次为 466,255 B → 467,441 B → 468,463 B → 容器类以当前为准；提交包 `复赛作品.zip` 依次为 44,430,861 B → 44,432,244 B → 44,432,240 B → 44,432,904 B → **最新以根目录 `hashes.txt` 为准**（`python pack_final.py` 重写它，`python hashes.py` 可抽查内容类）。

**其他状态未变**：冻结模型/验证集/训练脚本/冻结数字仍原样未动；高音归因仍为 F1=0（修复方案已选定 C，目标决赛路演 23 日前，尚未启动）；用户验证仍未开展（已如实披露）。
