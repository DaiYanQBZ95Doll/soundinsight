# 明日收尾清单（目标：9/14 全部搞定，截止 9/15）

> 当前状态：工程、验证、文档、提交包（除视频）**全部完成**；审计 0 FAIL / 143 PASS。
> 本清单只列"还没做的动作"，照做即可，不需要重新推导任何东西。

## 一、四步收尾

### 第 1 步：录视频（约 30-60 分钟）

1. 双击 `启动Demo.bat` → 等窗口出现 `* Running on local URL: http://127.0.0.1:7860` → 浏览器打开该地址
2. 打开 `video_script_silent.md`（拍摄卡：9 个镜头只写"点哪里、画面要出现什么"，**不需要说话**）
3. 按卡录屏（Win + G 或 OBS），得到一段或多段 mp4
4. 剪辑：新建剪映项目 → 导入录屏 → 导入 `视频素材/` 里 8 张 PNG（按 `视频素材/素材清单.md` 的时间轴摆）→「文本 → 本地字幕 → 导入字幕」选 `video_script.srt`（字幕自动对齐）
5. 导出 1080p / 30fps，命名 **`更新世界的锋芒_SoundInsight_演示视频.mp4`**，放到 `C:\deepseek-harness-master\soundinsight\`

### 第 2 步：打包（1 分钟）

双击 `打包提交包.bat`（等同 `python pack_final.py`）。看到四项全部 `[OK]` 即完成：

| 提交物 | 当前状态 |
|---|---|
| `..._复赛作品.pdf`（主文档 9 页，内嵌中文字体） | 已在包内 |
| `..._Demo.zip`（50 文件） | 已在包内 |
| `..._其他材料.zip`（46 文件 + 说明） | 已在包内 |
| `..._演示视频.mp4` | **待补（唯一缺口）** |

### 第 3 步：上传天池

- 只上传一个文件：`更新世界的锋芒_SoundInsight_复赛作品.zip`
- 命名与模板要求一致（团队名_方案名称_复赛作品.zip）

### 第 4 步：补推送（1 分钟，可选但建议）

```powershell
cd C:\deepseek-harness-master\soundinsight
git push origin main
```
（GitCode 已是最新；GitHub 落后若干 commit）

## 二、提交前 60 秒自查

- [ ] 浏览器打开在线 Demo（https://modelscope.cn/studios/DaiYanQBZ95Doll/SoundInsight）能正常判定 —— 免费实例空闲后会冷启动，**首次访问慢属正常**，提前点一次热身
- [ ] 包内 PDF 打开正常（团队信息表 / 在线链接表 / 注意事项 都在）
- [ ] 视频时长在 3-5 分钟
- [ ] zip 里没有"待放入/占位"字样文件
- [ ] 校验值变化属预期（视频入包后 SHA256 会变；`python hashes.py` 可重算）

## 三、可选收尾

- GitCode 访问令牌已在聊天中出现过，建议提交完成后在 GitCode 设置里**删除并重建**一个
- 若要给 Kimi 终审：把 `docs/D13_seal_declaration.md`（封包声明，含校验值与审核清单）交给它即可；视频入包后第三节校验值需按新值更新（可让 DSH 重算）

## 四、冻结提醒（明天不要动）

- 模型权重、`val_v2.csv`、训练脚本、`results_summary.md` 中的冻结数字：**一律不改**
- 视频字幕/画面里的数字已全部核对过冻结口径，**不要临场改数**
- 录屏必须是真实系统，不要用合成画面冒充实机演示
