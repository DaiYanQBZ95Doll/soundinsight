# 高音归因修复方案（treble_fix_proposal.md）

> 前置：失效机制已定位，见 `docs/treble_failure_analysis.md`（零触发：最大概率 0.3332 < 阈值 0.5）。
> 本文件给出**三档修复方案**，含实测收益、代价、以及"哪些东西仍然冻结"的边界。
> 触发条件：若入围决赛（用户决定）。当前仍处冻结状态，**未实施任何修复**。

## 一、方案 A：逐类阈值（不动权重，成本最低）

**做法**：多标签判定从"统一 0.5"改为逐类阈值，阈值作为配置项（`config.json`）而非硬编码。

**实测收益**（`tune_per_class_threshold.py`，验证划分 252 条）：

| 类别 | 统一 0.5 的 F1 | 最优阈值 | 逐类阈值 F1 |
|---|---|---|---|
| 低音 | 0.7945 | 0.20 | **0.8642** |
| 清晰度 | 0.7714 | 0.25 | **0.8313** |
| 杂音 | 0.8392 | 0.35 | 0.8400 |
| 音量 | 0.8356 | 0.60 | **0.8652** |
| **高音** | **0.0000** | **0.20** | **0.5161** |
| **宏 F1** | **0.6481** | — | **0.7834（+0.135）** |

- 需要改动：`config.json`（新增 `issue_thresholds`）、`predict_core.py` 与 `soundinsight_agent.py` 的判定处（读配置，缺省回退 0.5）。
- 不需要改动：模型权重、`val_v2.csv`、训练脚本——**不触碰任何冻结物**。
- 代价：约 1 小时（含改代码、回归测试、写文档）。
- 风险与披露要求：
  1. 阈值是在**同一验证划分**上网格搜索得到的 → 指标偏乐观；对外引用前必须改用**交叉验证或独立调参划分**重选阈值（否则属"在同一集合上调参并报数"）。
  2. 高音 FP 会上升（验证划分 8 TP / 10 FP），要在报告里写清"高音类误报增加"。
  3. `results_summary.md` 的 **0.6481（统一阈值）保持不动**，逐类阈值结果作为**新增指标行**单独记录。

## 二、方案 B：类别加权重训（需解冻，收益更实）

**做法**：多标签训练改用带 `pos_weight` 的 BCE（按各类负正比设定，高音权重最大），并改用**多标签分层**划分（iterative stratification，替代当前 `stratify=Y.argmax(axis=1)`）。

- 预期：高音召回提升，且大概率不必把阈值压到 0.20；其他四类基本不变或微升。
- 代价：1-3 小时（改训练脚本 + GPU 重训 ~5 分钟 + 评估 + 文档）。
- 必须遵守的边界：
  1. **不覆盖冻结模型**：新模型存到新目录（如 `multi_label_model_v2/`），旧模型与其指标原样保留；
  2. 用**独立划分**评估，并同时报告统一阈值与逐类阈值两套结果；
  3. 高音标签仍用 84 条还是改用补捞后的 124 条，需明确并披露（当前冻结模型是 84 版本）。
- 提醒：二分类上的消融 B 已证明 class_weight 会"过补偿"（精确率崩到 35.5%），多标签侧的权重需要重新调，不能照搬。

## 三、方案 C：标签重定义 + 重训（治本，成本最高）

**做法**：先治标签，再重训。
- 依据（实测）：高音正例 84 条中 **40.5% 同时标了清晰度**；含高音关键词的 206 条评论里 **129 条（62.6%）漏标高音**。
- 两条可选路线：
  - **合并**：把"刺耳/尖利"明确归入"清晰度"，取消独立高音类 → 宏 F1 直接受惠（剔除高音后四类宏 F1 = 0.8102）。代价是失去一个细分维度。
  - **细化高音定义**：明确"高音过亮/刺耳/齿音"的判据，对 206 条关键词评论做**重标 + 人工抽查**，再重训。
- 代价：0.5-1 天（重标需 LLM 复核 + 人工抽查；本轮已删除用户反馈环节，人工部分需用户本人参与）。
- 收益上限最高，且能顺带解决"清晰度被高音污染"的问题。

## 四、三档对比

| | A 逐类阈值 | B 加权重训 | C 标签重定义+重训 |
|---|---|---|---|
| 是否动权重 | 否 | 是（新目录） | 是（新目录） |
| 是否触碰冻结物 | 否 | 需解冻多标签训练 | 需解冻 + 重标 |
| 实测/预期宏 F1 | **0.7834（实测）** | 预计 0.78-0.82 | 0.80+（取决于标签质量） |
| 高音 F1 | **0.5161（实测）** | 预计 0.55-0.70 | 取决于合并/细化路线 |
| 耗时 | ~1 小时 | 1-3 小时 | 0.5-1 天 |
| 主要风险 | 同集调参偏乐观、FP 上升 | 权重过补偿；新旧模型并存需讲清 | 重标引入新的标注噪声 |

## 五、建议顺序（供决策，非强制）

1. 先做 **A**（当天可完成，且不改模型，风险最低）——先拿到"高音不再是 0"的可用产品行为；
2. 若时间允许，再做 **B**（让高音不必靠 0.20 这种激进阈值撑住）；
3. **C** 视决赛时间与人力决定；若做合并路线，需同步更新文档、PPT 与 Q&A 中"五类归因"的表述（改为四类或"四类 + 刺耳并入清晰度"）。

## 六、修完要同步的文件（清单）

- `results_summary.md`：**只增不改**——保留 0.6481，新增逐类阈值结果行（注明口径与调参方式）
- `MODEL_CARD.md` 第 7 条（高音机制）→ 更新为已修复状态与新技术指标
- `competition_v4.md` §7.7 多标签、§9.1 局限（高音条目）、§9.2 未来工作（移除已做项）
- 新增或更新：`docs/treble_failure_analysis.md`（补"修复后"一节）、`per_class_thresholds_probe.json`（换成 CV 口径的正式结果）
- `AI_HANDOFF/03_metrics_and_caveats.md`（补新指标行）、`AI_HANDOFF/06_pending_and_redlines.md`（待办勾掉）
- 审计：`python check_doc_numbers.py` 必须仍 0 FAIL（新数字需登记进 `check_doc_numbers.py` 的必含项）
- 若改动了 `predict_core.py`：跑一次本地 Demo + 在线 Demo（`deploy_check.py` / `batch_check.py`）确认无回归；创空间需重新推送部署包

## 七、当前状态（勿混淆）

- 模型仍冻结，高音 F1 **仍是 0**（本文档只是方案，未实施）
- 探针结果文件 `per_class_thresholds_probe.json` 是**同集调参的乐观值**，不可直接对外引用
- 冻结数字 `0.6481`（统一阈值 0.5）不得修改

## 八、决策记录

| 日期 | 决策 | 目标时点 | 状态 |
|---|---|---|---|
| D13 | **用户选择方案 C（标签重定义 + 重训）** | 决赛路演（23 日）之前 | **未启动**（用户明确"现在不着急行动"；且提交物（9/14）优先） |

## 九、方案 C 执行清单（待启动时照做）

### 9.1 先做一个二选一决策（需要用户拍板）

| 子路线 | 做法 | 代价 | 语义影响 |
|---|---|---|---|
| **C1 合并** | 把"刺耳/尖利"归入"清晰度"：`clarity_new = clarity ∨ treble`，五类 → 四类 | **低**：纯脚本合并标签，无需新增 LLM 复核；重训 + 评估约半天 | 失去"高音"细分维度；报告从五类变四类，PPT/Q&A/文档表述需同步改 |
| **C2 细化** | 保留五类，把"高音过亮/刺耳/齿音"判据写细，对 206 条含高音关键词的评论重标（其中 129 条疑似漏标） | **高**：LLM 复核 + 人工抽查（需用户本人参与，本项目不做 AI 代填）+ 重训，约 0.5-1 天 | 保留五类；有望把高音 F1 提到可用区间，但质量取决于重标一致性 |

> 重要口径提醒：`0.8102`（剔除高音后的四类宏 F1）**不等于** C1 合并后的宏 F1——合并会让"清晰度"标签集变化（从 565 条变成 565+84 的并集），清晰度 F1 需重测。不要把 0.8102 当作 C1 的预期结果对外说。

### 9.2 执行步骤（C1 / C2 通用）

1. **标签处理**
   - C1：脚本生成 `labeled_llm_v2.csv`，其中 `issue_clarity_llm = clarity ∨ treble`，并删除 `issue_treble_llm` 列；备份原文件。
   - C2：先用现有 LLM 复核能力对 206 条含高音关键词的评论做一次**判据明确的重标**（判据先写成文档，再执行），产出新标签列；随后由用户对其中抽样的 30-50 条做人工确认（本项目的诚信要求：不得 AI 代填人工结论）。
2. **划分与训练**
   - 重训多标签模型到**新目录** `multi_label_model_v2/`（**不得覆盖** `multi_label_model/`）；
   - 划分改用多标签分层（iterative stratification，替代 `stratify=Y.argmax(axis=1)`），或至少对稀少类做分层保证；
   - 训练配置同时试：普通 BCE 与带 `pos_weight` 的 BCE（对照，避免二分类消融 B 那种"过补偿"）。
3. **评估（严格口径）**
   - 用**独立划分或交叉验证**选阈值，再报指标；禁止"同集调参同集报数"；
   - 同时报统一阈值与逐类阈值两套结果，并披露 FP 变化；
   - 与旧模型（五类，统一 0.5，宏 F1 0.6481）并排对比，写清不可比之处（类别数/标签集不同）。
4. **产品接入**
   - `config.json` 增加类别阈值项；`predict_core.py` / `soundinsight_agent.py` / `demo_sound_v2.py` / 在线部署包 `deployment/app.py` 同步（C1 则同时改类别名与报告文案）；
   - 回归测试：本地 Demo（`deploy_check.py` 本地版）、线上（`batch_check.py`）；创空间需重新推送并重建。
5. **文档与审计同步**
   - `results_summary.md`：**只增不改**（保留 0.6481 与旧多标签结果，新增 v2 结果并注明口径）；
   - `MODEL_CARD.md`、`competition_v4.md` §7.7/§9.1/§9.2、`docs/treble_failure_analysis.md`（补"修复后"一节）、`AI_HANDOFF/03`（新指标行）、`AI_HANDOFF/06`（待办勾掉）；
   - 新数字登记进 `check_doc_numbers.py`，保持 0 FAIL；
   - 若 C1：全文检索"五类"表述，逐处改为四类或注明"刺耳并入清晰度"。

### 9.3 验收标准（建议）

- 高音（或合并后的清晰度）在独立口径下 F1 **≥ 0.5**，且宏 F1 不低于当前 0.6481；
- 五类（或四类）中不再存在 F1 = 0 的类别；
- 线上 Demo 回归通过（单条 + 批量），创空间部署成功；
- 审计 0 FAIL；文档中"五类归因"表述与实现一致。
