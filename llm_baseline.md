# LLM 对照实验报告（llm_baseline.md）

> 生成日期：2026-09（复赛冲刺）
> 实验目的：D6-D8 遗憾消除清单 Batch B —— 在固定 1000 条评估子集上，对比小模型（DistilBERT, thr=0.9744）与 LLM（deepseek-chat, temperature=0）三种提示设置：零样本（zero）、5 示例少样本（5-shot）、复核模式（review，附小模型预测作为参考）。

## 1. 实验设定（口径）

- 评估子集：`llm_eval_input.jsonl`，共 1000 条，正例 251 / 负例 749，seed=42，抽自 RLCA 弱标注池（`labeled_llm` 流程产出）。
- 小模型预测：`llm_eval_small_preds.jsonl`，阈值 0.9744，同子集。
- LLM：deepseek-chat，temperature=0，max_tokens=8000，批量 30–50 条/次，4 路并发调用。
- 重要口径声明：
  - 该子集来自弱标注池，小模型在训练阶段可能部分见过其中样本，因此小模型在本子集上的指标可能偏高（存在记忆效应上界）；LLM 未使用该池任何数据进行训练，指标无此效应。
  - 本子集指标 ≠ 冻结口径（val_v2 上 F1=0.687 等），仅用于同口径横向对比。

## 2. 结果总表（同 1000 条子集，如实记录）

| 设置 | Acc | Precision | Recall | F1 | TP | FP | FN | TN | tokens | 与模型预测不同的比例 |
|---|---|---|---|---|---|---|---|---|---|---|
| 小模型 (thr 0.9744) | 0.9250 | 0.9889 | 0.7092 | 0.8260 | 178 | 2 | 73 | 747 | 0 | — |
| LLM 零样本 | 0.9710 | 0.9784 | 0.9044 | 0.9400 | 227 | 5 | 24 | 744 | 81,388 | 7.6% |
| LLM 5 示例 | 0.9420 | 0.9707 | 0.7928 | 0.8728 | 199 | 6 | 52 | 743 | 83,196 | 7.9% |
| LLM 复核模式 | 0.9320 | 0.9791 | 0.7450 | 0.8462 | 187 | 4 | 64 | 745 | 85,119 | 2.9% |

- 三种 LLM 设置合计 tokens ≈ 249,703。另有一次 50 条零样本批次首轮解析失败（已用 batch=30 重试成功），该次调用 token 未计入上表，实际总消耗略高于此数（≤ 约 3k tokens 量级）。
- 成本估算：按 deepseek-chat 长期目录价（输入 $0.27/M、输出 $1.10/M）与实测 prompt/completion 比例（约 82%/18%）估算，3000 次判定（3 设置 × 1000 条）总成本约 **$0.1（约 ¥0.7）量级**。此为依据目录价的估算，实际以账单为准；价格如有调整以调用当日官方价为准。
- 延迟实测：单批 40 条 = 2,560 ms / 2,552 ms（两次独立探针）；1000 条 = 25 批 ≈ 65 秒（串行等效），4 路并发下每设置约 1 分钟完成。

## 3. 如实结论（不做粉饰）

1. **零样本 LLM 在本子集上明显优于小模型**：F1 0.940 vs 0.826（+0.114），差距主要来自召回（0.904 vs 0.709），精确率相当（0.978 vs 0.989）。小模型漏报的正例中相当一部分 LLM 能识别。
2. **5 示例反而比零样本差**（F1 0.873 < 0.940）。如实记录，不声称少样本有效；提示设置对该任务高度敏感。
3. **复核模式几乎不改变小模型判定**（仅 2.9% 翻转，F1 0.846 ≈ 小模型 +2 分）：把小模型预测作为参考交给 LLM 会产生明显锚定效应，复核模式的"独立第二意见"价值有限。
4. **工程含义**（与产品定位一致）：小模型的价值不在精度对标前沿 LLM，而在推理成本与吞吐（1000 条量级的本地推理毫秒/秒级，GPU/CPU 吞吐见 Batch E3 实测）；LLM 在标注管线中担任质检神谕（oracle）角色，与 RLCA 的"规则初筛 + LLM 全量复核"定位一致。
5. 本对照同时证明：小模型在本子集漏报（FN=73）多于误报（FP=2），与我们此前 val_v2 上的"高召回、低精确"分布方向不同——这再次说明**不同数据源/口径下的指标不可互相替代**，冻结口径仍以 `results_summary.md` 为准。

## 4. 复用

- 复算脚本：`llm_eval_metrics.py`（输入/输出路径内置，输出 `llm_eval_metrics.json`）。
- 重跑 LLM 需宿主侧 staged 工具 `llm_eval`（本机 DSH 宿主环境持有 API key）。
