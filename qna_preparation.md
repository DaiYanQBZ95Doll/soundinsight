# SoundInsight 答辩 Q&A 准备

## Q1：高音 F1 为什么是 0？
高音类样本天然稀缺（仅 84 条），且与"清晰度"语义重叠（如"shrill"可归两类）。当前召回不足，属数据分布局限而非架构缺陷。后续将通过类别重定义（将"刺耳"统一归入清晰度）解决。已在文档局限章节如实披露。

## Q2：LLM 复核精度 51.8%，1257 条有多少噪声？
51.8% 是规则初筛 1502 条的精度。三星补漏的 479 条经人工抽查验证，78% 准确。1257 条中约 20% 为边界噪声，但在 1:10 欠采样训练下，噪声影响被稀释。模型在独立验证集上 F1 0.69，证明信号足以压倒噪声。

## Q3：和直接用 GPT-4 比，小模型优势在哪？
GPT-4 单次调用约 $0.01/条，1000 条 $10，且需联网上传数据。我们的小模型本地推理 1000 条成本为 0，耗时 30 秒，隐私可控。F1 0.69 虽低于 GPT-4 的潜在上限，但满足了中小卖家"低成本、离线、批量"的核心诉求。

## Q4：验证集从 1000 条变 20000 条，结果可比吗？
初赛验证集 1000 条（13 正例）因正例过少导致 F1 波动极大。复赛重新分层划分 20000 条（251 正例），确保验证集能代表 1257 条正例的分布。两者都是固定验证集，结果不可直接数值对比，但趋势一致：清洗后数据显著优于弱标注。

## Q5：如果卖家只有 50 条评论，系统还能用吗？
50 条评论可直接用 LLM 零样本分析（已验证可用）。当评论积累到 500 条以上，建议启用小模型批量分析，成本更低、速度更快。系统支持两种模式的平滑切换。

## Q6：数据是 2014 年的，现在还有效吗？
澄清：本项目使用的是 McAuleyLab 2023 版数据集，评论时间戳已全部恢复，跨度 2000 年至 2023 年 3 月，并非 2014 年旧数据。我们做了真实的跨年份分桶验证：在固定验证集上按评论年份分桶（时间戳覆盖 19,996/20,000 条），2022 年桶 F1@0.5 为 0.80（2515 条中 42 条正例），2014-2022 各年桶 F1 在 0.44 至 0.88 之间波动，未发现随时间衰减的趋势。同时生成了 2021-2023 月度音质差评率趋势图，为后续批次级质量预警打下基础。诚实的边界是，2023 年桶仅 5 条正例，数值噪声大，实时评论流的持续验证仍是未来工作。

## Q7：阈值 0.97 会不会漏掉很多真实差评？
0.97 是验证集上 F1 最优的阈值，此时 Precision 66.3%、Recall 71.3%。如果卖家希望"宁可错杀不可放过"，可在 config.json 中调低阈值至 0.5，Recall 升至 89.6%，F1 降至 0.62。系统支持阈值自定义。

## Q8：多标签重叠怎么处理？一条评论可能同时有低音+杂音？
多标签模型采用独立分类头架构，每个类别独立预测概率。一条评论可以同时命中低音和杂音，报告中会分别列出。这种设计更符合真实场景，因为音质问题往往是复合的。

## Q9：和 Helium 10 有什么区别？
Helium 10 提供通用评论情感分析，但不支持音质专项识别和五类问题归因。SoundInsight 的差异化在于：切口更小（只聚焦音质）、归因更细（五类问题）、成本更低（本地推理零 API 费用）。

## Q10：两阶段标注的方法论贡献是什么？
传统人工标注成本高（20 小时/$300），纯规则标注精度低（51.8%），纯 LLM 标注成本高且精度不稳。RLCA 框架通过规则召回+LLM 精筛，将标注成本降至 2 小时/$50，精度提升至 78%，为低资源场景提供了可复用的标注范式。
