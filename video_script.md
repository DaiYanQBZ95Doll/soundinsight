# SoundInsight 演示视频分镜脚本（3 分钟版，符合模板建议的 3-5 分钟）

> 目标时长：约 190 秒（3 分 10 秒），可按需拉伸到 3-5 分钟（镜头 3、6、9 各留 10-20 秒弹性）。
> 与旧版（130 秒）的差异：新增"验证体系"与"局限性自述"两个镜头，并补充在线 Demo 的实测数据。

## 录制准备检查单

- [ ] 分辨率 ≥1080p，横屏 16:9
- [ ] 关闭无关窗口与通知，桌面整洁
- [ ] Demo 已启动（`python demo_sound_v2.py`），模型预载完成
- [ ] 演示数据使用 `sample_reviews_100.csv`
- [ ] 浏览器提前打开在线 Demo（https://modelscope.cn/studios/DaiYanQBZ95Doll/SoundInsight）
- [ ] 录屏软件就绪（OBS 或系统自带），麦克风测试完成，环境安静
- [ ] 如需展示报告，提前生成 `insight_report_v2.md` 或 `insight_report.xlsx`

## 分镜表

### 镜头 1：痛点（20 秒）

画面：屏幕录制，英文差评列表滚动（可用 `sample_reviews_100.csv` 打开预览）。

口播稿："跨境电商耳机卖家，每天面对几十到上百条英文差评。这条说低音不足，这条说电流声，这条是三星、看起来没事——但我们实测发现，三星评论里有 41.5% 其实是在抱怨音质。人工一条条翻，一上午就没了。"

### 镜头 2：上传与批量分析（15 秒）

画面：Demo「批量分析」页，拖入 `sample_reviews_100.csv`，点击生成。

口播稿："把评论导出成 CSV，拖进 SoundInsight，剩下交给模型。一百条评论，本地 GPU 一点八秒，网页版实测十五秒出结果——不联网、不上传、数据不出境。"

### 镜头 3：报告解读（40 秒）

画面：展示报告摘要，逐段指读（评论总数 / 差评占比 / 五类分布 / 优先级 / 典型案例）。

口播稿："报告出来了。评论总数、音质差评占比、五类问题分布，按优先级排好。低音问题最多，建议先查质检环节。每条差评带概率和原文，能直接点回去看。这是行动建议，可以照着做。也可以一键导出 Excel，交给运营和研发。"

### 镜头 4：单条判定与归因（20 秒）

画面：切到「单条评论」页，粘贴一条真实差评，点检测。

口播稿："单条也可以随时试。输入一条评论，模型给出是否音质负面，并归因到低音、清晰度、杂音、音量、高音五类。一条评论可以同时命中多类——我们的数据里，23.5% 的差评是复合问题。"

### 镜头 5：边界案例（25 秒）

画面：切换「边界案例」Tab，逐个展示六类场景。

口播稿："模型不是万能的，所以六类最容易出错的场景，我们直接做进了界面：委婉的差评、中性比较、关键词误触、评分冲突、多语言、极短评论。模型怎么判、概率多少、为什么，全部摆出来。知道边界在哪，才敢放心用。"

### 镜头 6：验证体系（35 秒）

画面：依次展示 `learning_curve.png`、`pr_curve.png`、`confusion_matrix.png`、`calibration_curve.png`。

口播稿："这套系统不是只跑了个模型。固定验证集两万条、五折交叉验证、三个消融实验、Welch t 检验 p 值 0.0009，显著优于 SVM 基线；PR 曲线 AUC 0.7191。我们还做了跨模型对照——同样的子集，deepseek-chat 零样本 F1 0.940、qwen3.7-plus 零样本 0.915，而本地小模型 0.826；也如实指出：大模型的标签红利来自它自己参与过标注，这个差异不能直接外推。校准评估发现概率在决策区间过度自信，所以界面上所有概率都标注了'未经校准，仅供排序参考'。"

### 镜头 7：在线 Demo（20 秒）

画面：浏览器打开公网地址，用手机流量访问，输入一条评论。

口播稿："这是在线版，评委现在就可以用手机打开，输入一条评论试试。空间已经发布，模型权重从公开仓库自动加载。"

### 镜头 8：局限性自述（15 秒）

画面：展示 `error_taxonomy.md` 与 `MODEL_CARD.md` 的局限章节。

口播稿："我们也把短板写在文档里：高音类样本太少，归因 F1 是 0；长评论超过 128 个 token 会被截断，那一档 F1 掉到 0.565；标注噪声率约 9.1%；还没有真实用户访谈。这些数字都以仓库里的验证报告为准。"

### 镜头 9：总结（10 秒）

画面：回到项目首页或 GitHub 仓库首页。

口播稿："SoundInsight——让每一条差评，都成为产品进化的信号。谢谢。"

## 时长合计

20 + 15 + 40 + 20 + 25 + 35 + 20 + 15 + 10 = **200 秒（3 分 20 秒）**，落在模板建议的 3-5 分钟区间内。

## 录制提示

- 若需压到 3 分钟整：镜头 3 压到 30 秒、镜头 6 压到 30 秒。
- 若需拉长到 4-5 分钟：镜头 3 展示完整 Excel 导出流程（+30 秒），镜头 6 逐张讲 PR 曲线与学习曲线（+40 秒），镜头 7 演示 Agent 命令行生成报告（+30 秒）。
- 所有口播数字均取自冻结口径（`results_summary.md`）与已完成的验证报告，**录制时不要临场改数字**。
