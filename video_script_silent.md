> **本片已按实拍时间轴录制完成（2026-09-14）**：成片 1920×1080 / 30fps / 3 分 23 秒；实际切点 0:20 / 0:33 / 1:11 / 1:42.080 / 2:18.140 / 2:38.140 / 3:03.010 / 3:23.010；字幕已重排为 26 条（末条 3:23.010）。本文档保留其计划用途，供后续版本复用。

# SoundInsight 演示视频·无口播版拍摄卡（3 分 13 秒）

> 适用：**不录音、不说话**，靠"屏幕录制 + 字幕"完成演示。
> 模板只要求「展示核心业务流程即可」，未要求配音，因此本方案完全合规。
> 配套文件：`video_script.srt`（字幕文件，可直接导入剪映/WPS 演示，自动贴到时间轴）。

## 一、录屏设置（1 分钟）

**启动 Demo 最简单的方式**：双击 `启动Demo.bat`（已内置"本地环回绕过系统代理"，避免 gradio 启动自检 502 退出）。窗口出现 `Running on local URL: http://127.0.0.1:7860` 后，浏览器打开该地址。

- 分辨率 1920×1080，横屏 16:9；关闭微信/QQ/邮件通知
- 录屏工具任选：Windows 自带 **Xbox Game Bar（Win + G → 录制）**、WPS 演示自带录屏、OBS
- 建议开启"鼠标点击高亮"（OBS/剪映录屏都有），观众能看清你点了哪里
- 录之前先把 Demo 跑热（模型加载约 20-40 秒），浏览器提前开好在线 Demo 页面

## 二、分镜操作卡（照做即可，无需台词）

| # | 时长 | 你要做的操作 | 画面里必须出现 |
|---|---|---|---|
| 1 | 0:00-0:20 | 打开 `sample_reviews_100.csv`（Excel/WPS 或记事本），用鼠标缓慢向下滚动英文评论列表 | 满屏英文差评，多条提到 bass / static / muffled |
| 2 | 0:20-0:28 | 切到 Demo「批量分析」页 → 拖入 `sample_reviews_100.csv` → 点"生成洞察报告" → 停在等待/结果 | 文件上传成功、按钮点击、结果出现 |
| 3 | 0:28-1:08 | 在报告区从上往下缓慢滚动，鼠标逐段停在"总体概况 / 问题分布表 / 典型案例 / 行动建议 / 验证指标 / 附注" | **完整六节报告**：分布表带占比与优先级（低音标"高"）、每条案例下列出"归因概率：杂音 0.93…"、行动建议列出紧急项 |
| 4 | 1:08-1:28 | 切到「单条评论」页 → 粘贴一条真实差评 → 点"检测" → 停在结果 | 判定＋概率＋问题归因文字（如"杂音"） |
| 5 | 1:28-1:53 | 切到「边界案例」页 → 缓慢滚动六类场景 | 委婉表达/中性比较/关键词误触/评分冲突/多语言/极短评论 |
| 6 | 1:53-2:28 | 依次打开图片：`learning_curve.png` → `pr_curve.png` → `confusion_matrix.png` → `calibration_curve.png`（每张停 8 秒） | 四张图表清晰可见 |
| 7 | 2:28-2:48 | 切到浏览器，打开在线 Demo（ModelScope 创空间地址），输入一条评论并回车；有余力再传一次 `sample_reviews_100.csv` 展示**线上同样是六节报告** | 上线地址栏 + 判定结果返回（线上批量页现与本地同口径） |
| 8 | 2:48-3:03 | 打开 `error_taxonomy.md` 与 `MODEL_CARD.md`，滚到"局限"章节 | 高音 F1=0、截断桶 0.565、噪声 9.1% 等文字 |
| 9 | 3:03-3:13 | 回到 Demo 首页或 GitCode 仓库首页 | 项目名 + 仓库地址 |

## 三、加字幕的两种做法（任选其一）

**做法 A：剪映（推荐，5 分钟）**
1. 新建项目 → 导入录制好的 mp4
2. 菜单「文本 → 本地字幕 → 导入字幕」→ 选 `video_script.srt`（自动对齐时间轴）
3. 调整字号（建议 36-42）、加半透明底、位置放画面下方
4. 导出：1080p / 30fps / mp4

**做法 B：WPS 演示（不用装剪映）**
1. 新建演示文稿 → 每页插入一段录屏片段（插入 → 视频 → 屏幕录制）
2. 每页加一个文本框写对应字幕（内容照 `video_script.srt` 抄）
3. 「输出为视频」→ 选 1080p → 导出 mp4

## 四、导出与命名（硬性要求）

- 文件名必须为：`更新世界的锋芒_SoundInsight_演示视频.mp4`
- 放到 `C:\deepseek-harness-master\soundinsight\` 目录下
- 时长控制在 **3-5 分钟**（本方案 3 分 13 秒，符合）
- 然后运行 `python pack_final.py` 自动并入提交包

## 五、诚实提醒

- 视频里出现的数字必须是冻结口径（本卡片与字幕文件里的数字均已核对过 `results_summary.md`），**临时改口播/字幕数字会造成文档与视频不一致**。
- 只录真实系统，不要用合成画面冒充实机演示。
