# 视频录制速查（一页版 · 录的时候开着看）

> 不用说话。字幕文件承担全部解说，你只需要按顺序操作屏幕。
> 详细分镜：`video_script_silent.md`｜字幕：`video_script.srt`（27 条，0:00-3:13）｜素材：`视频素材/`（8 张 1080p 满屏图）
> 总时长约 **3 分 13 秒**（镜头 2 已压到 8 秒：本地 GPU 1-2 秒即出结果）。

## ① 准备（2 分钟）

1. 双击 `启动Demo.bat` → 等窗口出现 `* Running on local URL: http://127.0.0.1:7860`
2. 浏览器打开 `http://127.0.0.1:7860`（**先点两下跑热**，别录的时候才等加载）
3. 另开标签页：在线 Demo https://modelscope.cn/studios/DaiYanQBZ95Doll/SoundInsight （也先点一下预热）
4. 把 `sample_reviews_100.csv` 复制到桌面（录的时候好拖）
5. 录屏：`Win + G`（或 OBS），1080p；**关掉微信/QQ/邮件通知**；建议开"鼠标点击高亮"
6. 打开本文件 + `视频_script_silent.md` 放在旁边照着做

## ② 录制（可分 9 段录，不必一镜到底）

| # | 时长 | 操作 |
|---|---|---|
| 1 | 0:20 | 打开 `sample_reviews_100.csv`，慢慢往下滚英文差评 |
| 2 | 0:08 | Demo「批量分析」拖入 CSV → 点"生成洞察报告" |
| 3 | 0:40 | 报告区从上往下滚：总体概况 → 问题分布表（带优先级）→ 典型案例（**含"归因概率"行**）→ 行动建议（三条紧急）→ 验证指标（含中置信提示）→ 附注 |
| 4 | 0:20 | 「单条评论」粘贴一条真实差评 → 点"检测" |
| 5 | 0:25 | 「边界案例」慢慢滚六类场景 |
| 6 | 0:35 | 展示 `视频素材/` 里 4 张验证图（学习曲线 / PR / 混淆矩阵 / 校准曲线） |
| 7 | 0:20 | 切到在线 Demo，输入一条评论；有余力再传一次 CSV 展示线上同样六节报告 |
| 8 | 0:15 | 打开 `error_taxonomy.md`、`MODEL_CARD.md`，滚到"局限"章节 |
| 9 | 0:10 | 回到 Demo 首页或 GitCode 仓库首页 |

## ③ 剪辑（剪映，约 10 分钟）

1. 新建项目 → 导入录屏片段（主轨，按上表顺序）
2. `视频素材/` 8 张 PNG 拖进素材区 → 按 `视频素材/素材清单.md` 的时间轴摆、拖右边缘调时长
3. 字幕：「文本 → 本地字幕 → 导入字幕」→ 选 `video_script.srt`（28 条自动对齐）
4. **标题卡（00）与结尾卡（07）放上层轨道**、缩到 3-4 秒（覆盖式，别当独立片段，否则字幕错位）
5. 导出 1080p / 30fps / mp4

（不装剪映：WPS 演示每页插录屏 + 文本框抄字幕 → 输出为视频）

## ④ 打包上传（3 分钟）

1. 导出文件命名为 `更新世界的锋芒_SoundInsight_演示视频.mp4`，放到 `C:\deepseek-harness-master\soundinsight\`
2. 双击 `打包提交包.bat` → 四项全 `[OK]`
3. 上传 `更新世界的锋芒_SoundInsight_复赛作品.zip` 到天池（只传这一个）

## ⑤ 两条铁律

- **字幕里的数字不要改**：全部取自冻结口径，改了会与文档/审计不一致
- **必须录真实系统**：本地 Demo、在线 Demo、真实报告与图表；不要用合成画面冒充实机
